## 2.2.6
- 用提交前历史边界排除旧结果，按 Flow 新结果置顶的顺序统计本批新增图片。
- 移除旧跟踪ID完整性、两秒一对一替换、旧失败必须为零和列表style必须相同等条件。
- 本次任一成功立即成功；有生成中或未确认图片则等待；本批全部失败稳定1.5秒后回写失败。
- 记录本批观测到的最大图片数，暂时少一张时不提前判失败。
- 卡片批量重建后重新统计本批结果，不要求逐张继承ID。

## 2.2.5
- 删除 Flow 结果关联中的提示词、标题、aria-label 文本匹配。
- 本次提交使用独立任务 token，每张卡片分配独立 UUID 跟踪 ID。
- 卡片的生成中、失败和成功检测基于跟踪对象及 DOM 状态。
- 可确认的一对一卡片替换跨观察回调延续同一个跟踪 ID；不按列表序号猜测历史卡片归属。
- 保留提交前基线，忽略旧成功图片和旧失败；缺失的已跟踪图片不会导致提前判全批失败。

## 2.2.4
- 每个 Flow 网页固定一次处理一条提示词。
- 本批任意图片成功即成功；仍有图片生成中时不判失败，全批失败后回写失败。
- 提交前失败卡片作为基线排除，允许有旧失败时关联新增失败；数量未增加的旧失败重挂载不会作为新失败。
- 已关联卡片不再因页面滚动位置改变而被直接忽略。
- 保留失败冷却两分钟、Opal 和其他功能。

# 2.2.3

仅修复Flow失败检测：根据用户失败页面补上匿名失败卡片的关联恢复，支持无尺寸组件内可见的失败面板。保留成功优先、失败冷却和全部其他运行功能。

55项回归检查通过。新增 tests/failure-page.cjs 使用本次附件解析的结果卡片结构，省略账户区域和真实资源URL。其余运行JavaScript文件与2.2.2逐字节一致。

安装：停止任务，覆盖原目录，重新加载扩展，刷新Flow页面。不要卸载原扩展。
